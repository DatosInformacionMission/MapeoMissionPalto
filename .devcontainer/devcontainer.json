{
  "name": "Flutter Dev Container",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.flutter",
        "Dart-Code.dart-code"
      ]
    }
  },
  "containerEnv": {
    "PATH": "/sdks/flutter/bin:${containerEnv:PATH}"
  },
  "onCreateCommand": "sudo mkdir -p /sdks && sudo chown vscode:vscode /sdks && git clone --depth 1 -b stable https://github.com/flutter/flutter.git /sdks/flutter",
  "postCreateCommand": "flutter pub get",
  "forwardPorts": [8080]
}
